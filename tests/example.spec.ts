import { test, expect } from '@playwright/test';

test.skip('example placeholder', async () => {
  // Kept as a placeholder so Playwright stays configured in the repo, but we avoid
  // hitting external sites during local/CI runs.
  expect(true).toBe(true);
});
